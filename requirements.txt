# ─── Core ────────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pyyaml>=6.0.1
rich>=13.7.0
psutil>=5.9.8

# ─── Document Loading ─────────────────────────────────────────────────────────
PyMuPDF>=1.24.0          # PDF: fitz
python-docx>=1.1.0       # DOCX
lxml>=5.1.0              # XML parsing

# ─── Chunking ────────────────────────────────────────────────────────────────
# (no extra deps — uses stdlib re + custom splitter)
# tiktoken is optional for exact token counting; we use char-based approx
# tiktoken>=0.7.0

# ─── Embeddings & Retrieval ──────────────────────────────────────────────────
sentence-transformers>=3.0.0
chromadb>=0.5.3

# ─── LLM (llama.cpp) ─────────────────────────────────────────────────────────
# On Jetson: build from source with LLAMA_CUDA=1 (see setup_jetson.sh)
# On dev (CPU/non-Jetson): install pre-built wheel
llama-cpp-python>=0.2.90

# ─── VLM ─────────────────────────────────────────────────────────────────────
transformers>=4.45.0
accelerate>=0.33.0
bitsandbytes>=0.43.3     # INT4 quantization via BnB
Pillow>=10.3.0
torchvision>=0.19.0      # required by Qwen2-VL

# ─── ASR ─────────────────────────────────────────────────────────────────────
faster-whisper>=1.0.3

# ─── TTS ─────────────────────────────────────────────────────────────────────
# AI4Bharat Indic TTS — offline VITS inference engine + model downloader
TTS>=0.22.0              # Coqui TTS (inference engine for AI4Bharat VITS models)
huggingface_hub>=0.23.0  # one-time model download from HuggingFace
sounddevice>=0.4.7
soundfile>=0.12.1

# ─── API ─────────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.1
python-multipart>=0.0.9
aiofiles>=23.2.1

# ─── OCR (optional) ──────────────────────────────────────────────────────────
# pytesseract>=0.3.10    # requires system tesseract installed
# paddlepaddle>=2.6.0    # PaddleOCR alternative (heavier, better accuracy)
# paddleocr>=2.7.3

# ─── Eval ────────────────────────────────────────────────────────────────────
numpy>=1.26.0
